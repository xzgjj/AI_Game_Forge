//! 数据库schema定义
//! 由Diesel自动生成

// 这个文件将由Diesel CLI自动生成
// diesel migration run 命令会生成实际的schema.rs文件

// 临时占位内容
pub mod users {
    // 用户表schema将在这里定义
}

pub mod projects {
    // 项目表schema将在这里定义
}

pub mod ai_logs {
    // AI日志表schema将在这里定义
}

pub mod game_specs {
    // 游戏配置表schema将在这里定义
}

pub mod api_stats {
    // API统计表schema将在这里定义
}

pub mod auth_sessions {
    // 认证会话表schema将在这里定义
}
