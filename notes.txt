# AI Game Forge 开发笔记
最后更新：2026-04-02

## 2026-04-02 设计协作与用户流补齐
1. **当前前端流转已补齐**：登录 -> 仪表盘 -> 配置向导 -> AI 画布 -> 返回仪表盘。
2. **Figma 设计协作基线已新增**：`doc/figma_ui_spec.md` 作为页面设计、反馈和验证点的统一参考。
3. **项目文档需要同步**：`README.md`、`notes.txt`、`diff.md` 已被更新以反映新的设计协作入口。
4. **后续协作原则**：先按文档核对页面和流转，再决定是否继续把当前 4 页工作台扩展回完整 5 页工作台。
5. **历史产品定义保留**：旧的 5 页 / 8 阶段定义继续保留在 `implementation_plan.md` 与历史记录中，避免丢失产品语义。

## 当前状态审查（新规划对齐）
1. **用户页面已重新定义**：前台不是线性 8 步向导，而是 5 个用户页面，分别承载新建项目、资源导入、内容画布、协作优化和发布版本记录。
2. **内部阶段与页面必须分离**：8 个阶段保留为内部生成与校验流水线，不再等同于 UI 页面数量。
3. **当前实现仍偏旧模型**：现有 `ConfigWizard` 和 `WizardState` 仍是线性表单与扁平快照，暂时不够支撑画布节点、版本分支和对象级 diff。
4. **Unity 对接仍需补强**：目前可用的是初始化、UPM 注入、基础校验和可选 BatchMode 验证，还缺少资源同步、最小场景验证、回滚与发布审计。
5. **新增内容必须版本化**：角色、台词、资源和场景都应作为可追踪对象，新增内容不能直接覆盖旧版本。

## 已确认的核心目标
- 固定 5 个用户页面，页面职责清晰，状态变化和用户反馈可见。
- 8 个内部阶段用于实现与校验，不与页面数量混淆。
- 自动对接 Unity（项目模板 + 脚本包 + 资源同步 + 校验 + 回滚）。
- AI 生成能力必须覆盖：玩家控制、敌人 AI、碰撞、状态机、核心循环、数值、技能，并且能落到内容画布上的对象节点。

## 近期优先级（高）
1. **先冻结方案**：确认 5 个页面职责、对象模型和版本语义，不急着改 UI。
2. **状态模型重构**：拆分 `ProjectState`、`WorkspaceState`、`ContentNode`、`VersionSnapshot`。
3. **页面重构**：把当前线性 wizard 改成 5 页工作台，先补页面壳和状态切换，再补内容。
4. **内容画布**：落地角色卡、台词卡、关系线、局部重生成和最小场景验证。
5. **Unity Bridge**：
   - 项目模板与 Scene 初始化
   - UPM 脚本包注入
   - manifest/GUID 映射
   - 资源同步、验证与回滚
6. **版本治理**：
   - diff 记录
   - snapshot
   - 回滚
   - 发布审计

## 本次补充
- 新增 `doc/web_ux_review.md`，用于核对 5 个页面的布局、用户行为、反馈和验证点。
- 该文档用于和页面设计对齐，重点检查“用户看见什么、点击后发生什么、失败后怎么回退”。
- `doc/AI_Game_Forge_Report.md` 已同步改写为 Web 工作台版总报告，避免旧版 8 步说法继续传播。

> 下列 2026-03-14 及更早条目保留为历史记录，当前产品定义以本文件顶部与 `implementation_plan.md` 为准。

## 本次更新（2026-03-14）
1. ConfigWizard 已升级为 8 步流程并加入步骤校验与进度导航。
2. 新增 WizardState 结构化产物类型，供后续持久化与审计使用。
3. Unity Bridge 最小闭环已落地：项目模板生成、UPM 注入、静态校验。
4. Step3/Step4 已接入 Unity Bridge 调用，前端具备最小执行链路。
5. Step3/Step4 增加本地持久化与可选 BatchMode 编译校验。
6. WizardState 已写入 SQLite，并新增“加载上次 WizardState”入口。
7. 新增按项目路径加载 WizardState 入口。
8. 补齐 src-tauri/src/lib.rs，修复 cargo test 入口。
9. 修复 Cargo 依赖配置（tauri feature 与 ollama-rs）。
10. 修复 `cargo test` 编译并添加占位图标（测试通过但有警告）。
11. 使用 `assets/logo.png` 生成正式图标，更新 `src-tauri/icons/icon.ico` 与新增 `icon.png`。
12. 重新拆解并补全 8 步任务清单，补充各阶段目的与理解。

## 中期优先级
1. 角色/资源生成管线（Step5/6）
2. 玩法迭代记录与参数调优（Step7）
3. 打包发布与版本回滚（Step8）

## 风险提醒
- 文档与实现必须同步更新，避免再次出现“文档虚高”。
- Unity 导入环节是可用性关键点，必须优先完成。

---
*每次流程或架构变更后更新此文件*
